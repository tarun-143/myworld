fav_colors={
	'hcdb':'hdcbh',
	'jdne':'kdmce',
	'jnd':'ieied',
	'eeded':'dvf'
}
lists=fav_colors.items()
for key,value in fav_colors.items():
	print(f'fav color of {key}  is {value} .')
fav_colors_keys=fav_colors.keys()
print(fav_colors_keys)
fav_colors_values=fav_colors.values()
print(fav_colors_values)
